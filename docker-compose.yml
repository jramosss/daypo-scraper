services:
  daypo-scraper:
    build: .
    container_name: daypo-scraper
    restart: always
    ports:
      - "8005:8005"
    volumes:
      - ./cuestionarios.db:/app/cuestionarios.db
    # Habilitar entrada interactiva si es necesario
    stdin_open: true
    tty: true

volumes:
  db_data:
